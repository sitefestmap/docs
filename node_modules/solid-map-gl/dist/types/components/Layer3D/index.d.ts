import { VoidComponent } from 'solid-js';
declare global {
    interface Window {
        BABYLON?: any;
        THREE?: any;
    }
}
type Props = {
    id?: string;
    origin?: [number, number, number?];
    defaultLight?: boolean;
    babylon?: boolean;
    onAdd?: (scene: any, map: any, gl: any) => void;
    onRender?: (gl: any, matrix: any) => void;
};
export declare const Layer3D: VoidComponent<Props>;
export {};
