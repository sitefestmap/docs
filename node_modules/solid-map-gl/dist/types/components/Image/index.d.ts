import { VoidComponent } from 'solid-js';
import type { StyleImageInterface, StyleImageMetadata } from 'mapbox-gl/src/style/style_image';
export declare const patternList: string[];
export type Color = `#${string}` | `rgb(${number}, ${number}, ${number})` | `rgba(${number}, ${number}, ${number}, ${number})` | `hsl(${number}, ${number}%, ${number}%)` | `hsla(${number}, ${number}%, ${number}%, ${number})`;
type Props = {
    id: string;
    /** The unique identifier for the image. */
    source?: HTMLImageElement | ImageBitmap | ImageData | SVGElement | {
        width: number;
        height: number;
        data: Uint8Array | Uint8ClampedArray;
    } | StyleImageInterface | string;
    /** The image to be used for the image component. */
    options?: StyleImageMetadata & {
        fill?: Color;
        stroke?: Color;
        transform?: string;
    };
    /**  The options for the image */
    pattern?: {
        type: string;
        color: Color;
        background: Color;
        lineWith: number;
    };
};
export declare const MGL_Image: VoidComponent<Props>;
export {};
